{
	"name": "trainrides",
	"version": "0.0.0",
	"private": true,
	"packageManager": "pnpm@10.21.0",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"check": "tsc --noEmit && tsc --noEmit -p test/tsconfig.json",
		"test:seed": "node --test ../scripts/build-seed.test.ts"
	},
	"devDependencies": {
		"@cloudflare/vitest-plugin": "^1.0.0",
		"@types/node": "^26.4.1",
		"typescript": "^5.5.2",
		"vitest": "~4.1.0",
		"wrangler": "^4.129.0"
	},
	"dependencies": {
		"hono": "^4.13.7"
	}
}
